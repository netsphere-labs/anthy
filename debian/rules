#!/usr/bin/make -f
# Uncomment this to turn on verbose mode.
# export DH_VERBOSE=1

%:
	dh $@ --with quilt

override_dh_auto_configure:
	dh_auto_configure -- --sysconfdir=/etc/anthy

override_dh_auto_clean:
	rm -f alt-cannadic/Makefile.orig src-util/Makefile.in
	debconf-updatepo
	dh_auto_clean
